import { VuetifyPreset } from 'vuetify/types/presets'

const options: VuetifyPreset = {
  theme: {
    dark: true,
    themes: {
      dark: {
        primary: '#854feb'
      }
    }
  }
}

export default options
